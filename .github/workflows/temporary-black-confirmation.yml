name: Temporary Confirmation Formatter

on:
  push:
    branches:
      - fix/confirmation-key-diagnostics

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/confirmation-key-diagnostics
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Black
        run: python -m pip install black
      - name: Format
        run: black -l 80 analytics_mcp tests horizon_server.py
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analytics_mcp tests horizon_server.py
          if git diff --cached --quiet; then
            echo "No formatting changes required."
          else
            git commit -m "style: format confirmation diagnostics"
            git push origin HEAD:fix/confirmation-key-diagnostics
          fi
